# Configuration

Runtime configuration is centralized in `src/core/configuration.mjs`.

Environment names supported by P0:

- development
- test
- staging
- production

Production is not configured or deployed by P0.
